import React from 'react'
// import { useSelector } from 'react-redux'

const SocialsComponents = () => {
  // const { socials } = useSelector(state => state.profile)
  return (
    <div>SocialsComponents</div>
  )
}

export default SocialsComponents