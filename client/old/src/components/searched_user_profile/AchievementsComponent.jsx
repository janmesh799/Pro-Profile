import React from 'react'
// import { useSelector } from 'react-redux'

const AchievementsComponent = () => {
  // const { achievement } = useSelector(state => state.profile);
  return (
    <div>AchievementsComponent</div>
  )
}

export default AchievementsComponent